A simple program to help master the syntax of Assemmbly language if you try to compile this in Windows it won't work use linux to run in simple steps
Convert the Assembly code -> Object file then to an Executable
as stringOutput.s -o stringOutput.o
ld stringOutput.o -o stringOutput
